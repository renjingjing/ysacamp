<p id="notice"><%= notice %></p>

<h1>Listing Classtimetables</h1>

<table>
  <thead>
    <tr>
      <th>Start time</th>
      <th>End time</th>
      <th>Start day</th>
      <th>End day</th>
      <th>Weekday</th>
      <th>Month</th>
      <th>times_per_week</th>
      <th colspan="3">Edit</th>
    </tr>
  </thead>

  <tbody>
    <% @classtimetables.each do |classtimetable| %>
      <tr>
        <td><%= formatted_time(classtimetable.start_time) %></td>
        <td><%= formatted_time(classtimetable.end_time) %></td>
        <td><%= formatted_date(classtimetable.start_day) %></td>
        <td><%= formatted_date(classtimetable.end_day) %></td>
        <td><%= classtimetable.weekday %></td>
        <td><%= classtimetable.month %></td>
        <td><%= classtimetable.times_per_week %></td>
        <td><%= link_to 'Show', classtimetable %></td>
        <td><%= link_to 'Edit', edit_classtimetable_path(classtimetable) %></td>
        <td><%= link_to 'Destroy', classtimetable, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Classtimetable', new_classtimetable_path %>
